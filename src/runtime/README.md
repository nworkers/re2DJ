# src/runtime

런타임 코어 구현이 들어갈 자리입니다. 아직 비어 있습니다.

*Implementation of the runtime core. Empty for now.*

PE32 적재(섹션 매핑·재배치·import 바인딩), 게스트 주소 공간, x86-32 인터프리터 backend가 순서대로 들어옵니다. 설계는 [ARCHITECTURE.md](../../ARCHITECTURE.md) 7절입니다.

*PE32 loading (section mapping, relocation, import binding), the guest address space, and the x86-32 interpreter backend land here in that order. See section 7 of [ARCHITECTURE.md](../../ARCHITECTURE.md).*
