# Windows-First Execution Policy

## 한국어

Windows-first 정책을 반영했습니다. 64-bit Windows는 1차 개발·통합 검증 호스트이고, Linux는 Windows 단계 완료 후의 이식 대상으로 명시했습니다. TODO의 Linux native helper 항목도 완료로 갱신했습니다.

다음 구현은 Windows helper에서 원본 `ez2dj1.exe`를 적재해 import gate 호출을 관찰하는 작업입니다.

## English

Applied the Windows-first policy. 64-bit Windows is the primary development and integration-validation host, while Linux is explicitly a porting target after Windows stages. The completed Linux native-helper TODO item was updated.

The next implementation task loads original `ez2dj1.exe` in the Windows helper and observes import-gate calls.
